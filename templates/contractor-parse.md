Извлеки следующие поля из данных контрагента: {{FIELDS}}
Проверяй форматы полей по справочнику выше.
{{CONTEXT}}

Входные данные:
{{INPUT}}

Верни ТОЛЬКО JSON-объект с извлечёнными полями.
Для ненайденных полей используй пустую строку.
В числовых полях (inn, passport_series, passport_number, bank_account, bik, corr_account, ogrnip, passport_code) оставляй только цифры и допустимые разделители (дефис в коде подразделения).
Если контрагент прислал ФИО в формате «Имя Фамилия», переставь в формат «Фамилия Имя Отчество».
Если данные неполные, извлеки то, что есть. Не придумывай недостающие значения.
Если заметишь что-то подозрительное (опечатка, нетипичное значение, несоответствие длины), добавь поле "comment" с кратким пояснением на русском.
