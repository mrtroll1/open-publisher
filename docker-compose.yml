services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./service_account.json:/app/service_account.json:ro
      - ./business_config.json:/app/business_config.json:ro
